services:
  postgres:
    image: postgres:18-alpine
    container_name: mvst-postgres-prod
    restart: unless-stopped
    env_file:
      - .env.production
      - .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-mvst_coffee_db}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - postgres_data_prod:/var/lib/postgresql/data
    networks:
      - edge
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      cache_from:
        - node:24-alpine
    image: mvst-backend:prod
    container_name: mvst-backend-prod
    restart: unless-stopped
    env_file:
      - .env.production
      - .env
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      BACKEND_PORT: ${BACKEND_PORT:-5000}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-mvst_coffee_db}
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      FRONTEND_URL: ${FRONTEND_URL}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - edge
    labels:
      caddy: ${CADDY_BACKEND_LABEL:-mvst-api.nugo.dev}
      caddy.reverse_proxy: "{{upstreams ${BACKEND_PORT:-5000}}}"
    command: >
      sh -c "
        npx prisma migrate deploy &&
        npm run start:prod
      "

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      cache_from:
        - node:24-alpine
    image: mvst-frontend:prod
    container_name: mvst-frontend-prod
    restart: unless-stopped
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      NEXT_PUBLIC_API_URL: ${BACKEND_URL}
      PORT: ${FRONTEND_PORT:-3000}
    depends_on:
      - backend
    networks:
      - edge
    labels:
      caddy: ${CADDY_FRONTEND_LABEL:-mvst.nugo.dev}
      caddy.reverse_proxy: "{{upstreams ${FRONTEND_PORT:-3000}}}"

volumes:
  postgres_data_prod:

networks:
  edge:
    external: true
